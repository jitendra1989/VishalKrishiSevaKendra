$('.new_category').remove();
<% if @category.root? %>
	$('.categories').append('<%= j (render 'category', category: @category ) %>');
<% else %>
	$('#category_<%= @category.parent.id %> > ul').append('<%= j (render 'category', category: @category ) %>');
<% end %>
