<% if @category.root? %>
	$('.parent-category > ul .list-group-item:last-child').html('<%= j (render @category ) %>');
<% else %>
	$('.new_category').html('<%= j (render 'category', category: @category ) %>').removeClass('new_category');
<% end %>
