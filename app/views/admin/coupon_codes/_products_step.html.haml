.row
  = f.collection_check_boxes(:product_ids, Product.pluck(:id, :name), :first, :second) do |product|
    .col-md-3
      .form-group.styled-checkboxes.text-center
        = product.check_box
        = product.label
      %br