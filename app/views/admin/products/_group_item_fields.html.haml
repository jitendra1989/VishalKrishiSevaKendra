.nested-fields.form-inline
  .form-group
    = f.select :related_product_id, Product.simple.pluck(:name, :id)
  .form-group
    = f.number_field :quantity, placeholder: 'Quantity'
  %span
    = link_to_remove_association f, class: 'delete-link' do
      %span.glyphicon.glyphicon-remove
