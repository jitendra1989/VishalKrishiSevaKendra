%tr.nested-fields
	%td
		= f.select :product_id, Product.pluck(:name, :id), include_blank: 'Select Product'
		= link_to_remove_association f do
			%span.glyphicon.glyphicon-remove.delete-link.pull-right
	%td= f.number_field :quantity, class: 'quantity-field', min: 1
	%td Rate
	%td Amount
