.category-nav
	%ul.list-unstyled
		- Category.roots.each do |category|
			%li.root-category
				= link_to front_category_path(category) do
					= category.name
				- if category.has_children?
					%ul
						- category.children.each do |category|
							%li
								= link_to category.name, front_category_path(category)