Thank you for your order as follows:
= "\n\n"
- @order.items.each do |item|
	= "#{item.name} X #{item.quantity} @ #{number_to_currency item.price} = #{number_to_currency item.quantity * item.price}"
	= "\n\n"
= "\n\n"
= "Total #{number_to_currency @order.subtotal}"
= "\n\n"
- @order.taxes.each do |tax|
	= "#{tax.name} #{number_to_currency tax.amount}"
	= "\n\n"
= "Final Total #{number_to_currency @order.total}"
= "\n\n"
Please find attached a copy of your invoice.
= "\n\n"
= "\n\n"
